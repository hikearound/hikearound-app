export const config = {
    radius: 30,
    maxZoom: 10,
    padding: 10.5,
};

export const queryParams = {
    sortDirection: 'desc',
    querySize: 25,
    queryType: 'map',
    lastKey: null,
    distance: 50,
    filterParams: null,
};
