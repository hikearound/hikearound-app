export const HIKE_NOTES = {
    LOADING_STATE:
        'This story demonstrates the loading state of the Hike component. When loading is true, it shows a loading indicator while the map data is being fetched.',
    LOOP_TRAIL:
        'This story shows a loop trail route. The trail starts and ends at the same point, creating a circular path. You can adjust the distance and elevation using the knobs.',
    OUT_AND_BACK_TRAIL:
        'This story displays an out-and-back trail route. The trail goes from point A to point B and then returns along the same path. You can modify the distance and elevation using the knobs.',
};

export const RECENT_REVIEW_LIST_NOTES = {
    DEFAULT:
        'This story shows the default state of the RecentReviewList component with a list of reviews.',
    TRANSPARENT:
        'This story demonstrates the RecentReviewList with a transparent background.',
    EMPTY: 'This story shows the empty state of the RecentReviewList when there are no reviews to display.',
};

export const RECENT_REVIEW_LIST_ITEM_NOTES = {
    DEFAULT:
        'This story shows the default state of the RecentReviewListItem component with a single review.',
    WITH_LIKES:
        'This story demonstrates the RecentReviewListItem with likes enabled.',
};

export const HEADER_NOTES = {
    DEFAULT:
        'The default header component with customizable title and no top border. Use the knobs to modify the title and toggle the top border.',
    WITH_TOP_BORDER:
        'This variant shows the header with a top border. The border helps visually separate the header from the content below it.',
    LOGGED_OUT:
        'This variant demonstrates the header in a logged-out state. The header adjusts its appearance and functionality when the user is not logged in.',
};
